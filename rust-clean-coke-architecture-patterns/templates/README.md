# Templates

These templates are domain-agnostic. Copy the file into your project and replace the placeholder names (Project/Item/Task) with your domain terms. Keep the route->usecase->repository structure intact.
