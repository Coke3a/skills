# Documentation Workflow Is Out Of Scope

This Rust Clean Architecture skill does not define a general documentation workflow.

Use concise comments only when they clarify non-obvious architecture boundaries, such as
why a type belongs in a specific layer or why a conversion is intentionally one-way.
